# Morphodynamics experiments

This group tests constraints on local terrain evolution in isolated benchmark
source. Local-incision variants can optionally regenerate the PPM captures in
`tests/generated/morphodynamic-stability/`; talus relaxation is still active.

Result: no tested `MAX_LOCAL_INCIDENCE` satisfies all criteria at 5000. Talus
relaxation has no benchmark conclusion yet. Neither mechanism changes production.
