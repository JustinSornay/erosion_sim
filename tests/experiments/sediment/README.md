# Sediment experiments

This group evaluates isolated alternatives to historical suspended-sediment
handling: conservative flux-form transport, its performance, and exchange laws.
Each script reads production source and injects its candidate only in memory.

Result: TRANSPORT B fixes conservation but remains morphologically insufficient
downstream. EXCHANGE C is rejected as the dominant explanation; equivalent
MASS_TARGET and CONCENTRATION_RELAXATION formulations pass equivalence checks.
